{
  "name": "claude-trees",
  "version": "0.1.0",
  "private": true,
  "description": "Visualise the hidden branch tree of a claude.ai conversation.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build --config vite.config.content.ts && vite build --config vite.config.background.ts",
    "dev": "npm run build -- --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
